# board.h is the copied board selection, remade by `make` from board_$(BOARD).h.
# The narray-generated files (src/g431/, src/l432/) and the lib copies in
# src/nlib/ are NOT ignored: they are the vendored [N]Array sources this repo
# builds from, refreshed only by `make -C src n-array-clean`. See src/Makefile.
src/board.h
# build outputs
src/*.o
src/*.elf
src/*.bin
src/*.hex
src/*.map
src/bme280_comp_test
# pdftotext extracts (regenerable from the PDFs)
doc/*.txt
src/bme280_comp_test.dSYM/
